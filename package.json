{
  "name": "purrmadeath",
  "version": "0.0.1",
  "description": "2D roguelike factory infinite game with cats",
  "scripts": {
    "start": "parcel index.html --host localhost --port 3001 --open",
    "start:lan": "parcel index.html --host 0.0.0.0 --port 3001",
    "build": "parcel build index.html --dist-dir dist",
    "multiplayer:server": "node server/multiplayerServer.js",
    "multiplayer:test:load": "node server/tools/loadTestBots.js",
    "multiplayer:test:sync": "node server/tools/syncProbe.js",
    "multiplayer:test:fault": "node server/tools/faultProbe.js"
  },
  "dependencies": {
    "pixi.js": "^8.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  }
}
